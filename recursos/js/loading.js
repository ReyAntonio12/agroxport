   /* PRELOADER SCREEN */
   window.addEventListener('load', () => {
    document.querySelector('.load').classList.add('load--hidden');
});   /* PRELOADER SCREEN */
window.addEventListener('load', () => {
    document.querySelector('.load').classList.add('load--hidden');
});